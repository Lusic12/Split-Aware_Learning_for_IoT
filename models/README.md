# Models

- `define_model.py`: model factory by name (cnn/rnn/lstm/gru/tcn_transformer/mlp/dnn/rad_ffnn).
- `student_model.py`: student architectures for distillation.

Used by `dl_main.py` and `Distillation_main.py`.
